version: 2

sources:
  - name: dataset_dart_samsung
    schema: raw
    description: dataset for dart api, samsung
    freshness:
      warn_after:
        count: 24
        period: hour
    tables:
      - name: raw_customers
        description: One record per person who has purchased one or more items